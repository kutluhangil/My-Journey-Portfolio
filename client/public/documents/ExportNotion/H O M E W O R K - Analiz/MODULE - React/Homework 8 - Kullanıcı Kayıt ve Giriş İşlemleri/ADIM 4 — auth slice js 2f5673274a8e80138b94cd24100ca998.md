# ADIM 4 — auth/slice.js

## **1️⃣ Dosyayı oluştur**

**YOL BİREBİR:**

```
src/redux/auth/slice.js
```

---

## **2️⃣ Başlangıç State (ÖDEVDE VERİLEN YAPI)**

Ödevin istediği yapı **AYNEN** bu:

```
{
  user: {
    name: null,
    email: null,
  },
  token: null,
  isLoggedIn: false,
  isRefreshing: false,
}
```

---

## **3️⃣ KOPYALA – YAPIŞTIR (TAM HALİ)**

```
import { createSlice } from "@reduxjs/toolkit";
import { register, login, logout, refreshUser } from "./operations";

/*
  AUTH INITIAL STATE
  Kullanıcı bilgileri + token + login durumu
*/
const initialState = {
  user: {
    name: null,
    email: null,
  },
  token: null,
  isLoggedIn: false,
  isRefreshing: false,
};

/*
  AUTH SLICE
  reducers YOK ❌
  sadece extraReducers var ✅
*/
const authSlice = createSlice({
  name: "auth",
  initialState,
  reducers: {},
  extraReducers: builder => {
    builder

      /* =========================
         REGISTER
         ========================= */
      .addCase(register.fulfilled, (state, action) => {
        state.user = action.payload.user;
        state.token = action.payload.token;
        state.isLoggedIn = true;
      })

      /* =========================
         LOGIN
         ========================= */
      .addCase(login.fulfilled, (state, action) => {
        state.user = action.payload.user;
        state.token = action.payload.token;
        state.isLoggedIn = true;
      })

      /* =========================
         LOGOUT
         ========================= */
      .addCase(logout.fulfilled, state => {
        state.user = { name: null, email: null };
        state.token = null;
        state.isLoggedIn = false;
      })

      /* =========================
         REFRESH USER
         ========================= */
      .addCase(refreshUser.pending, state => {
        state.isRefreshing = true;
      })
      .addCase(refreshUser.fulfilled, (state, action) => {
        state.user = action.payload;
        state.isLoggedIn = true;
        state.isRefreshing = false;
      })
      .addCase(refreshUser.rejected, state => {
        state.isRefreshing = false;
      });
  },
});

export const authReducer = authSlice.reducer;
```

---

## **🧠 BU DOSYADA NE OLUYOR? (NET ANLATIM)**

### **🔹 register.fulfilled**

- Backend’den user + token gelir
- Redux state’e yazılır
- Kullanıcı **login olmuş sayılır**

### **🔹 login.fulfilled**

- Register ile aynı mantık
- State güncellenir

### **🔹 logout.fulfilled**

- User sıfırlanır
- Token silinir
- isLoggedIn → false

### **🔹 refreshUser**

📌 **EN KRİTİK KISIM**

| **Durum** | **Ne olur** |
| --- | --- |
| pending | isRefreshing = true |
| fulfilled | user yüklenir, login true |
| rejected | refresh biter ama login olmaz |

Bu sayede:

- Sayfa yenilenince
- Token varsa
- Kullanıcı **otomatik login olur**

---

## **✅ CHECKLIST UYUMLULUK**

✔ createSlice kullanıldı

✔ initialState birebir doğru

✔ reducers **YOK**

✔ extraReducers var

✔ 4 async işlem bağlandı

✔ authReducer export edildi

---

## **❌ EN SIK YAPILAN HATALAR**

❌ reducers yazmak

❌ refreshUser.pending eklememek

❌ isRefreshing unutmak

❌ logout’ta state temizlememek

---

## **⛔ ŞU AN NE EKSİK?**

Henüz:

- selectors ❌
- persist (store.js) ❌
- routing ❌

Ama **auth logic artık hazır** 🧠🔥

---