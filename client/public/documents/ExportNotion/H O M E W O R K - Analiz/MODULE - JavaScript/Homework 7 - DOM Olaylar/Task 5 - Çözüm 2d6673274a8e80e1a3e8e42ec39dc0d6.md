# Task 5 - Çözüm

**Task-5 Çözümü (task-5.js)**

```
const button = document.querySelector(".change-color");
const colorSpan = document.querySelector(".color");
const body = document.body;

function getRandomHexColor() {
  return `#${Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, 0)}`;
}

button.addEventListener("click", () => {
  const randomColor = getRandomHexColor();

  body.style.backgroundColor = randomColor;
  colorSpan.textContent = randomColor;
});
```

---

## **🧠 Mantık – Kısa ve net açıklama**

### **1️⃣ Gerekli DOM öğelerini seçiyoruz**

```
const button = document.querySelector(".change-color");
const colorSpan = document.querySelector(".color");
const body = document.body;
```

✔ Buton

✔ Span

✔ Body

---

### **2️⃣ Rastgele renk üreten fonksiyon**

```
function getRandomHexColor() {
  return `#${Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, 0)}`;
}
```

✔ Hex formatında renk üretir

✔ Görevde **zorunlu**

---

### **3️⃣ Butona tıklanınca çalışacak olay**

```
button.addEventListener("click", () => {
```

✔ Renk **sadece tıklamada** değişir

---

### **4️⃣ Renk üret, uygula ve yazdır**

```
const randomColor = getRandomHexColor();

body.style.backgroundColor = randomColor;
colorSpan.textContent = randomColor;
```

✔ <body> arka planı değişir

✔ <span> aynı renk değerini gösterir

✔ Hex → body’de rgb görünmesi **normal** (mentor notu ✔)

---

## **🎯 Mentor Checklist (tam kontrol)**

✔ Arka plan sadece butona tıklayınca değişir

✔ Her tıklamada yeni rastgele renk

✔ <body> ve <span> aynı renk değerini alır

✔ getRandomHexColor() kullanıldı

✔ Konsolda hata yok

💯 **Task-5 TAM PUAN**

---

## **🧪 Beklenen davranış**

- Sayfa açıldığında → renk yok (-)
- **Change color** tıkla →
    - Arka plan değişir
    - Span’da örn: #3fa9d1 yazar
- Tekrar tıkla →
    - Yeni renk

---