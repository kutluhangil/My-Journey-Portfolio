# Task 1 - Çözüm

Aşağıda **mentor kontrolündeki tüm maddeleri %100 karşılayan**, **sadece gerekli yerlerde this eklenmiş** ve **istenen çağrılar aynen bırakılmış** **doğru JS çözümü** var.

> ❗ Önemli:
> 

> – Konsoldaki çağrılara
> 
> 
> **dokunulmadı**
> 

> – Sadece eksik this ifadeleri düzeltildi
> 

> – Kod mentor testlerinden geçecek şekildedir
> 

---

## **task-1.js – DOĞRU ÇÖZÜM**

```
const customer = {
  username: "Mango",
  balance: 24000,
  discount: 0.1,
  orders: ["Burger", "Pizza", "Salad"],

  // Change code below this line
  getBalance() {
    return this.balance;
  },

  getDiscount() {
    return this.discount;
  },

  setDiscount(value) {
    this.discount = value;
  },

  getOrders() {
    return this.orders;
  },

  addOrder(cost, order) {
    this.balance -= cost - cost * this.discount;
    this.orders.push(order);
  },
  // Change code above this line
};

customer.setDiscount(0.15);
console.log(customer.getDiscount()); // 0.15

customer.addOrder(5000, "Steak");
console.log(customer.getBalance()); // 19750
console.log(customer.getOrders()); // ["Burger", "Pizza", "Salad", "Steak"]
```

---

## **Neden Bu Çözüm Doğru?**

- this → metodu çağıran **customer nesnesini** temsil eder
- balance, discount, orders → **nesneye ait özelliklerdir**
- this kullanılmazsa → JS bu değişkenleri **global scope**’ta arar ❌

---

## **📌 Mentor Kontrol Listesi (Tek Tek)**

| **Kontrol Maddesi** | **Durum** |
| --- | --- |
| customer tanımlı | ✅ |
| Nesne özellik & metod içeriyor | ✅ |
| getBalance → doğru değer | ✅ |
| getDiscount → doğru değer | ✅ |
| setDiscount → güncelliyor | ✅ |
| getOrders → dizi döner | ✅ |
| addOrder → bakiye + sipariş | ✅ |
| Tüm metodlarda this var | ✅ |

---

## **🧠 Mini Ezber (Sınavlık)**

```
Nesne metodu → this.property
Global → property ❌
```

---