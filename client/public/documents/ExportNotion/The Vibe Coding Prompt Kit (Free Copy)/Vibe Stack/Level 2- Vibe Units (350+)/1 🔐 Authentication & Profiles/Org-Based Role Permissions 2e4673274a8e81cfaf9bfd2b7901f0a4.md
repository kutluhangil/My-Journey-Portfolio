# Org-Based Role Permissions

🪄 Prompt: Set up a system where user roles and permissions are scoped to their organization (e.g., Admin in Org A, Viewer in Org B).
✅ Includes: ✅ Role Management, ✅ Org Logic, ✅ Access Control
📝 Description: Sets up permissions based on the organization a user belongs to, so their role can be different in each workspace.
#: 39