# Swagger'ı API

Biz, Swagger'ı API'miz için belge oluşturma aracı olarak inceleyeceğiz.

Swagger ile rahat bir çalışma için `@redocly/cli` kütüphanesini kullanacağız. Bu kütüphaneyi yüklemek için aşağıdaki komutu çalıştırabilirsiniz:

`npm i -D @redocly/cli`

Kurulumun ardından, `package.json` dosyasındaki `scripts` alanına aşağıdaki komutları ekleyebilirsiniz:

```
// package.json

{

/* Dosyanın diğer kodları */

	"scripts": {
		"build": "npm run build-docs",
    "build-docs": "redocly bundle --ext json -o docs/swagger.json",
    "preview-docs": "redocly preview-docs",
   }
}
```

**`preview-docs`** Bu komut, dokümantasyonu yazarken değişikliklerin otomatik olarak yenilenmesini sağlar. (Tıpkı nodemon gibi çalışır.) Daha fazla bilgi için [buraya](https://redocly.com/docs/cli/commands/preview-docs/) göz atabilirsiniz.

**`build-docs`** Bu komut, `docs/swagger.json` dosyasını oluşturur. Bu nedenle, dokümantasyonun yazılacağı klasörü oluşturup içine aşağıdaki gibi boş bir JSON dosyası eklemeniz gereklidir:

```
// docs/swagger.json

{}
```

Redocly CLI ile çalışmak için, proje kök dizininde `redocly.yaml` adında bir yapılandırma dosyası oluşturmalısınız. Bu dosya, API dokümantasyonu oluşturma parametrelerini, stil ayarlarını, temaları ve diğer yapılandırmaları tanımlar. Aşağıda örnek bir yapılandırma verilmiştir:

```
# redocly.yaml

# See <https://redocly.com/docs/cli/configuration/> for more information.
apis:
  sample@v1:
    root: docs/openapi.yaml
extends:
  - recommended
rules:
  no-unused-components: error
theme:
  openapi:
    htmlTemplate: ./docs/index.html
    theme:
      colors:
        primary:
          main: '#32329f'
    generateCodeSamples:
      languages: # Array of language config objects; indicates in which languages to generate code samples.
        - lang: curl
        - lang: Node.js
        - lang: JavaScript
```

```
Daha önce .yaml uzantılı dosyalarla çalışmadık. Bu, daha basit ve okunabilir bir sözdizimine sahip JSON dosyalarının bir eşdeğeridir.
YAML (Yet Another Markup Language), insan tarafından okunabilir bir veri serileştirme formatıdır. Yapılandırma dosyaları ve uygulamalar arasında veri iletimi için kullanılır.
```

Bu dosyada, **`docs/index.html`** ve **`docs/openapi.yaml`** adlı dosyaları da oluşturmamız gerektiğini görüyoruz. Bu dosyaları aşağıdaki içerikle oluşturalım:

```
<!-- docs/index.html -->

<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>API Reference | ReDoc</title>
  <!-- needed for adaptive design -->
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" type="image/png" href="favicon.png">

  <!--
    ReDoc uses font options from the parent element
    So override default browser styles
    -->
  <style>
    body {
      margin: 0;
      padding: 0;
    }</style>{{{redocHead}}}
</head>

<body>{{{redocHTML}}}
</body>

</html>
# docs/openapi.yaml

openapi: 3.1.0
info:
  version: 1.0.0
  title: Students app
  license:
    name: Apache 2.0
    url: <http://www.apache.org/licenses/LICENSE-2.0.html>
  description: >
    This is a documentation of students app
tags:
  - name: Students
    description: Operations about users.
  - name: Auth
    description: Auth operations.
servers:
  - url: <http://localhost:3000>
  - url: <https://example.com/api/v1>
paths:
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
```

Ayrıca dokümantasyonu yazmaya başlamadan önce, VSCode için [**Redocly OpenAPI**](https://marketplace.visualstudio.com/items?itemName=Redocly.openapi-vs-code)uzantısını yüklemenizi öneririz: