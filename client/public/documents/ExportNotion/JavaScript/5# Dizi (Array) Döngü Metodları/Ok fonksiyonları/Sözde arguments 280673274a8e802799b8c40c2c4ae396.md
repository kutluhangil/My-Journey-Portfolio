# Sözde arguments

Ok fonksiyonları, `arguments` içeren bir yerel argüman değişkenine sahip değildir. Tüm argümanları bir dizide toplamanız gerekiyorsa, `rest` işlemini kullanın.

```
const add = (...args) => {
  console.log(args);
};

add(1, 2, 3);// [1, 2, 3]
```